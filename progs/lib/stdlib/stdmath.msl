!guard stdmath ; include guard for the label "stdmath"

; the fn_ is a matter of naming convention, it is not important to the compiler
; curly brackets are to put the function at the end of code

[add a b:
	{add_data: 0}
	
	a add_data >
	add_data b >
]

!endguard